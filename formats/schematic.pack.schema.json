{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "soveraeign.schematic/pack@0.1",
  "title": "SOV Schematic Pack (minimal envelope)",
  "description": "Documentation only: validation is the hand-written loadPack in src/07-state-space.js. The minimal envelope the state space carries until the domain pack format (issue #4) absorbs it. The built-in vocabulary ships as data/core.logic.pack.json.",
  "type": "object",
  "required": [
    "format",
    "id",
    "version",
    "definitions"
  ],
  "properties": {
    "format": {
      "const": "soveraeign.schematic/pack@0.1"
    },
    "id": {
      "type": "string",
      "minLength": 1
    },
    "version": {
      "type": "integer",
      "minimum": 1
    },
    "definitions": {
      "type": "array",
      "items": {
        "$ref": "schematic.definition.schema.json"
      },
      "description": "each id@version at most once"
    }
  },
  "additionalProperties": false
}
