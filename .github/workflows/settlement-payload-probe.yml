name: settle-beta24
on:
  push:
    branches: [rc/0.1.0-rc1]
    paths:
      - '.github/workflows/settlement-payload-probe.yml'
permissions:
  contents: write
jobs:
  staging:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Beta.24 authoritative ZIP transport staging'
